class Solution {
    public boolean isPalindrome(int x) {
       String xStr = Integer.toString(x);
       String reversedStr = new StringBuilder(xStr).reverse().toString();
       return xStr.equals(reversedStr); 
    }
}
